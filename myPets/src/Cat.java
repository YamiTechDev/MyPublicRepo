public class Cat {
    // 1. Свойства (переменные) — то, что у кота ЕСТЬ
    String name;
    int age;

    // 2. Поведение (методы) — то, что кот УМЕЕТ делать
    void meow() {
        System.out.println(name + " говорит: Мяу!");
    }
}
